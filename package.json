{
  "name": "nodejs-ts-express-puppeteer_rent-finder-api",
  "version": "1.0.0",
  "description": "Rent Finder Api using Puppeteer Scraping",
  "main": "./build/api/server.js",
  "scripts": {
    "start": "npm run build && node .",
    "dev": "PORT=5000 nodemon",
    "build": "rimraf ./build && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rodrigo G. Oliveira",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^20.4.1",
    "@types/puppeteer": "^7.0.4",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.1",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^20.8.0"
  }
}
